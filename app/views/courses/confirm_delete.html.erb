<table class="confirm-delete-table" border=1>
  <tr>
    <th>course_id</th>
    <td><%= @course.course_id %></td>
  </tr>
  <tr>
    <th>course_title</th>
    <td><%= @course.course_title %></td>
  </tr>
  <tr>
    <th>topic</th>
    <td><%= @course.topic %></td>
  </tr>
  <tr>
    <th>price</th>
    <td><%= @course.price %></td>
  </tr>
  <tr>
    <th>level_id</th>
    <td><%= @course.level_id %></td>
  </tr>
  <tr>
    <th>category</th>
    <td><%= @course.category %></td>
  </tr>
</table>

<%= link_to "delete", @course, method: :delete, data: {confirm: "You sure?" } %>
